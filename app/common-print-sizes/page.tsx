import { SizesTool } from '@/src/components/CalculatorClient';
export const metadata={title:'Common Print Sizes Library',description:'Reference common print sizes in inches, cm, mm and pixels.'};
export default function Page(){return <SizesTool/>}
