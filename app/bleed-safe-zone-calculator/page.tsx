import { BleedTool } from '@/src/components/CalculatorClient';
export const metadata={title:'Bleed & Safe Zone Calculator',description:'Calculate full bleed canvas, safe area, trim size and pixel margins.'};
export default function Page(){return <BleedTool/>}
