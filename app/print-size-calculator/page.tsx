import { PrintSizeTool } from '@/src/components/CalculatorClient';
export const metadata={title:'Print Size Calculator',description:'Convert pixels to inches, cm, mm and required pixels by DPI.'};
export default function Page(){return <PrintSizeTool/>}
