import Link from 'next/link';
import type { Metadata } from 'next';
import { coreTools, guides, templates } from '@/src/lib/content';
import { siteUrl } from '@/src/lib/site';
const description = 'Free print-ready calculators for image size, DPI, bleed, safe zones, KDP covers, Etsy printable files and PixelFit creator overlays.';
export const metadata: Metadata = { title: 'Print Ready Tools | DPI, Bleed, KDP and PixelFit Calculators', description, alternates: { canonical: siteUrl }, openGraph: { title: 'Print Ready Tools', description, url: siteUrl, siteName: 'Print Ready Tools', type: 'website' } };
export default function Home() { return <main className="container stack"><section className="hero"><p className="small"><strong>Free print-ready workflow hub</strong></p><h1>Print Size, DPI, Bleed & PixelFit Image Tools</h1><p>Calculate pixels, print dimensions, DPI/PPI, paper sizes, bleed, safe zones, KDP covers, Etsy printables, aspect ratios and social media overlay previews before printing or uploading.</p></section><section className="grid">{coreTools.map(([href, title, desc]) => <Link className="card" href={href} key={href}><h2>{title}</h2><p className="muted">{desc}</p></Link>)}</section><section className="grid"><div className="card"><h2>Guides</h2>{guides.slice(0, 6).map(([href, title]) => <p key={href}><Link href={href}>{title}</Link></p>)}</div><div className="card"><h2>Templates</h2>{templates.map(([href, title, desc]) => <p key={href}><Link href={href}>{title}</Link><br /><span className="muted small">{desc}</span></p>)}</div></section></main>; }
